#include <stdio.h>
int l;
int main() {
int a;
    for(a=0; a <= 11; a++){
        // printf("val of a is %d\n",a);
        printf("Hello\n");
    }
    printf("val of a outside the for loop is %d\n",a);
}